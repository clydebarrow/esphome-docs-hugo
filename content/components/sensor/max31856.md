---
description: "MAX31856 Thermocouple Temperature Sensor"
title: "MAX31856 Thermocouple Temperature Sensor"
---

{{< seo description="" image="" >}}

The `MAX31856` temperature sensor allows you to use your MAX31856 Thermocouple
temperature sensor ([datasheet](https://datasheets.maximintegrated.com/en/ds/MAX31856.pdf)) with ESPHome.

{{< img src="max31856-full.jpg" alt="Image" caption="MAX31856 Sensor. Image by `Adafruit`_" width="50.0%" class="center" >}}

[Adafruit](https://www.adafruit.com/product/3263)

As the communication with the MAX31856 is done using SPI, you need
to have an [spi bus](components/spi#spi) in your configuration with both **miso_pin** and **mosi_pin** set.

- `VIN` connects to 5V (`3V3` will output 3.3V), or directly connect `3V3` to 3.3V
- `3Vo` not used by ESPHome
- `GND` connects to ground
- `CLK` connects to the SPI **clk_pin**
- `SDO` connects to the SPI **miso_pin**
- `SDI` connects to the SPI **mosi_pin**
- `CS` connects to a free GPIO pin
- `FLT` not used by ESPHome
- `DRDY` not used by ESPHome

```yaml
# Example configuration entry
sensor:
  - platform: max31856
    name: BBQ Temperature
    icon: mdi:hamburger
    cs_pin: GPIOXX

```
## Configuration variables:

- **cs_pin** (**Required**, [Pin Schema](guides/configuration-types#config-pin_schema)): The Chip Select pin of the SPI interface.
- **update_interval** (*Optional*, [Time]({{< ref "guides/configuration-types#config-time" >}})): The interval to check the sensor. Defaults to `60s`.
- **mains_filter** (*Optional*, string): The mains power frequency to reject (`50 Hz` or `60 Hz`). Defaults to `60 Hz`.
- **thermocouple_type** (*Optional*, string): The type of thermocouple used. MAX31856 supports: B, E, J, K, N, R, S, and T . Defaults to `K`.
- **spi_id** (*Optional*, [ID]({{< ref "guides/configuration-types#config-id" >}})): Manually specify the ID of the [SPI Component](components/spi#spi) if you want to use multiple SPI buses.
- All other options from [Sensor](components/sensor/_index#config-sensor).

## See Also

- [Sensor Filters]({{< ref "components/sensor/_index#sensor-filters" >}})
- {{< docref "dht/" >}}
- {{< docref "adc/" >}}
- {{< docref "max6675/" >}}
- {{< docref "max31855/" >}}
- {{< docref "max31865/" >}}
- [MAX31856 Library](https://github.com/adafruit/Adafruit_MAX31856) by [Adafruit](https://www.adafruit.com/)
- {{< apiref "MAX31856/MAX31856.h" "MAX31856/MAX31856.h" >}}


